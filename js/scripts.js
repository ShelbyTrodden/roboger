// User Interface Logic
window.addEventListener("load", function(event) {

  
     
    
  form.addEventListener("submit", function() {
    event.preventDefault();
  
  
    });
    
  resetBtn.addEventListener("click", function() {
       
      });
    });

numIdentify(number); {
    if (number.includes(1, 2, 3)); {
        return true;
} return false; 
};